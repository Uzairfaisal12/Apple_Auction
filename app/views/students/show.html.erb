<h1> <%= @student.name%></h1>

  <div>
    <%= @student.number%>
  </div>

<%= link_to "Edit Student data",edit_student_path(@student) %>

<%= button_to "Delete", student_path(@student), method: :delete %>
